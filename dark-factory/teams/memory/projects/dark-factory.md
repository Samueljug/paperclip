# Dark Factory Project Memory

- 2026-06-12 route correction: Samuel superseded the Gemini-only worker rule. Use Codex for Dark Factory worker execution and coordination where available; non-Gemini lanes do not need Samuel approval merely for being non-Gemini. Do not wait on Gemini or Claude availability when Codex can move the work. OPE-201/OPE-205 are in_review with missing improver coverage disposition blockers and native Codex worker Euclid active; coordinate/source-of-truth check at least hourly and record exact owner/action blockers on Paperclip. Any handoff mentioning `stage` must include the Stage Repository Source Hard Rule: main app stage repos are `aila-quillio/quillio-backend` and `aila-quillio/quillio-frontend` on branch `stage`, not `aila-code` unless explicitly legal/dev app.
- 2026-06-12 OPE-420 full LFD PR perfection gate: Dark Factory PR work now optimizes for zero residual PR loss rather than throughput or PR count, and PR quality is not a checklist. Every PR/work loop needs a visible four-part loss function: (1) target with exact outcome plus eval/edge/adversarial cases that cannot be memorized by a tiny checklist; (2) constraints including time budget, scope boundary, allowed repos/providers/tools/models, security/tenant/data limits, cost budget when relevant, and forbidden shortcuts; (3) instruments/harness mapping every target/constraint to concrete evidence such as tests/CI, review-thread status, conflict check, security/tenant checks, browser/UX/pixel evidence, migration/data checks, long-run time/token/cost accounting, and improver/no-op coverage; (4) forced entropy when stalled or making marginal movement so the next attempt must use a new hypothesis, non-obvious alternative, or removal of eval-shaped artifacts rather than the same idea harder. Implementation self-score is never enough; use independent verification and hidden/adversarial/verifier-owned checks where practical. If a fix learns the eval instead of solving the general problem, block and repair. Each substantial loop records hypothesis, expected failure mode, diagnostic, metric/evidence delta, and next move. Missing, unknown, unverified, or non-zero residual loss blocks merge-ready, Ship-to-PR, Done, and final reporting. Codex-only route applies for this PR lane unless Samuel explicitly changes it; verify `gh api user --jq .login == Samueljug` before any GitHub write.
- 2026-06-10 OPE-39/OPE-201 productivity-review pattern: visible `Review productivity for ...` alerts and Foreman handoff comments are not enough. A source/child issue is still stale if it remains `in_progress` with no active run/execution participant and no fresh evidence/decision after handoff. Foreman recovery should produce a visible liveness decision: active run + next action, progress ledger/comment + next action, first-class blocked owner/action, reassign/re-slice/requeue, or healthy/duplicate closure with evidence. Downstream child blockers should be explicit, not only implicit prose references.
- 2026-06-10 OPE-39 split-routing lesson: before handing a split parent to workers, Foreman should map child dependencies and approval gates. Do not queue all children as parallel runnable work when a root child (e.g. OPE-151 privacy contract/schema) needs approval/import resolution; keep downstream children visible but explicitly dependency-blocked, then wake/requeue them when the root decision resolves. Handoff-only Foreman claims must not move dependency-blocked children back to `in_progress` unless a live execution path exists; if a child stays handoff-only `in_progress` across two watcher cycles, treat it as status-repair/escalation, not another handoff. Once blocker links and owner/action comments exist, watcher output should switch to root-decision reminder language rather than generic idle implementation alerts.
- 2026-06-10 OPE-201 routing/security lesson: privileged routing/escalation must not trust mutable descriptive fields such as agent names, role text, capabilities, labels, or free-form comments. Use board/server-managed permissions, stable IDs, and managed metadata; exclude source-assignee self-routing unless explicitly authorized; require adversarial tests for spoofed text, non-invokable agents, budget constraints, and cross-company boundaries.
- 2026-06-10 OPE-208 Browser QA lesson: for Expo/mobile QA, record native simulator availability (`xcrun simctl`, Android `emulator`) before web fallback; capture true app crash screenshots before route interception; mark shimmed or harness-bypassed web fallback screenshots as non-PASS supporting evidence; simple Paperclip comments should use `{body}` only unless API docs support more fields.
- 2026-06-11 OPE-213 authenticated Browser QA lesson: authenticated-flow QA must not advance from fixture-only/storage-state evidence or a browser still on `/login`. Use the existing local sandbox credential source by path/section only, perform visible UI login in a fresh browser context, prove authenticated content and reload persistence, and publish only redacted/shareable artifacts while keeping originals/trace/video local-only unless Samuel approves sharing.
- 2026-06-11 OPE-209 Paperclip finalization lesson: normal comments posted after issues were patched to `done` can reopen closed issues. For Option B/finalization flows, post required visible comments and attachments before closure, make the status patch to `done` the final mutation, then perform only read-only verification. If a post-closure comment is unavoidable, prove a no-reopen route first or explicitly plan a final status restore plus read-only recheck as the last step.
- 2026-06-12 clarified delegated-work follow-up standing rule, superseding the OPE-249 hourly wording: follow-up is event-first, with a 30-minute fallback watchdog. When Samuel gives OpenClaw/factory a task and it is delegated to any worker, team, cron job, watcher, monitor, or factory lane, do not treat handoff delivery or a quiet watcher as progress. Act immediately on material replies, progress, failure, completion, or blockers, and report to Samuel immediately when an update is material, completion evidence is ready, or his decision/action is genuinely required. The 30-minute check is only the fallback watchdog for no trustworthy update, stale handoff, or unclear progress: verify source-of-truth state and confirm the task is actually moving. If incomplete or stale, follow up with the current owner, unblock, reroute, retry/restart/respawn, fix the handoff, or escalate the exact Samuel-needed blocker every 30 minutes until it is complete, explicitly blocked on Samuel, cancelled, or superseded. Record current stage, active owner, blocker/none, next artifact due, and whether implementation is proceeding. Prior pending Skill Workshop proposal: `hourly-openclaw-task-followup-20260611-e9eba296e0`; the 30-minute proposal supersedes it and should include this event-first clarification.
- 2026-06-13 OPE-464/OPE-155 recurrence: after OPE-39 final disposition and merged PR stack, Paperclip missing-disposition recovery/Foreman reactivated OPE-155 and spawned a high-churn productivity review. Current cleanup pattern is: verify parent Done + merged PR/ledger evidence, post visible self-improvement/source disposition, resolve recovery/productivity review false_positive/Done, clear stale stage labels, and track the tooling fix in OPE-465.
- 2026-06-13 productivity-review cleanup pattern: source-state comments can reopen Done cards, so finalization order matters. Post manager/improver comments and artifacts first, then resolve recovery/set final status and only read-check afterward. OPE-408 is locally satisfied by the model-preset status proof but remains blocked on OPE-444 external PR #32 merge authority; do not rerun worker handoffs while that blocker is unchanged.
- 2026-06-11 OPE-311/OPE-312 DeepSec lesson: security-report runs need direct implementation pickup immediately after reviewer triage classifies confirmed blockers. Triage is not remediation progress by itself; the next artifact must be an accepted batch plan, implementation diff, verification/security evidence, PR URL, or one exact blocker. Runtime QA blocker lesson from OPE-291: authenticated browser matrices need a pre-approved dev-only subscribed QA/session fixture plus redaction contract before QA begins.
- 2026-06-13 OPE-262/OPE-40 native-hook recurrence: if delegated native subagents repeatedly fail with `Native hook relay unavailable` while the coordinator shell is healthy, stop retrying the same native-worker path after two failures and reroute through a reliable execution lane (detached Codex CLI/full-shell or verified worker) with saved logs and PR source-truth checks. Treat further recurrences as evidence that the previously transient OPE-40 tooling class needs a concrete automatic fallback/tooling fix.
- 2026-06-15 OPE-528 PiTeam route-drift lesson: the repair target is broader than one timed-out run. Dark Factory needs a factory-level guard that checks PiTeam route metadata (`codex_local` + `all-codex` + `codex` + `gpt-5.4`), stale queued/active work under drifted routing, and missing visible improver coverage after timeouts, then collapses the incident into one actionable health item before clearing stale red agent state. Current-state proof can come from `/api/companies/{companyId}/agents`; historical drift claims should stay explicitly bounded when config-revision history is not accessible from the executing agent.
- 2026-06-15 OPE-529/OPE-236 exact-head coverage lesson: terminal improver must treat `brief-artifact-manifest` plus `coverage-matrix` as the authoritative scope record for final source coverage. If exact-head build/verify/security passes only cover the original defect while accepted-plan rows remain uncovered, the source gets `improvement_review` + `lesson_recorded`, not a no-op, and the remaining No Mistakes/pre-PR/waiver owner path stays explicit on the source issue.
- 2026-06-15 OPE-512/OPE-239 blocker-ticket lesson: first-class blocker tickets need their own terminal improver coverage. If the source stays blocked on a real external contract/approval/owner action, self-improvement should record terminal coverage on the blocker ticket, cite current source truth plus missing superseding evidence, and move the blocker ticket to explicit `blocked`; otherwise the board can misread the source as blocked only by missing improver coverage.
