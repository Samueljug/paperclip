# Verification Lead Memory

- 2026-06-12 delegated-work follow-up correction: follow-up is event-first, with a 30-minute fallback watchdog. If any worker/watcher/monitor/lead/source-of-truth check reports material progress, failure, completion, or a blocker, act immediately and report to Samuel immediately when material/completion/decision-needed. Use the 30-minute check only when no trustworthy update arrives, handoff may be stale, or progress is unclear. At watchdog checks, verify source-of-truth movement and unblock/reroute/restart/fix/escalate exact Samuel-needed blockers. Stop only when the original outcome is complete, cancelled, superseded, or concretely Samuel-blocked.
- 2026-06-17 verification timeout lesson: verification agents must target only the specific files or test suites that have been modified or are relevant to the change (e.g. running only `vitest run src/__tests__/issue-done-guard-routes.test.ts`) rather than defaulting to running the entire codebase test suite, which takes too long and exceeds the heartbeat timeout limit.
- 2026-06-12 OPE-420 full LFD PR gate: Dark Factory PR verification is Codex-only unless Samuel explicitly changes it. No merge-ready/final/Ship/Done claim without visible four-part LFD (target, constraints, instruments/harness, forced entropy) and PR Loss Report showing zero residual loss. Implementation self-score is insufficient; use independent/verifier-owned/adversarial checks where practical. Unknown, stale, unverified, hand-waved, eval-shaped, failing, or non-zero residual loss blocks readiness. Each substantial loop needs an iteration log: hypothesis, expected failure mode, diagnostic, metric/evidence delta, next move. Verify `gh api user --jq .login == Samueljug` before GitHub writes; do not merge without Samuel.
