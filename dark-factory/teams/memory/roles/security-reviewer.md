# security-reviewer role notes

- 2026-06-12 OPE-420 full Loss Function Development (LFD) gate is active for Dark Factory PR cleanup and future PRs. PR quality is not a checklist: every PR/work loop needs a visible four-part loss function: target, constraints, instruments/harness, and forced entropy. Security review is part of the Instruments/Harness and anti-cheating layer. For security lane reviews, do not give PASS/merge-ready/final language unless residual security/tenant/data-exposure loss is zero and the visible PR Loss Report also accounts for privacy constraints, migrations/backwards compatibility, product correctness, CI/tests, review comments, conflicts, browser/UX where relevant, scope creep, and self-improvement/no-op coverage. Unknown/unverified categories are blockers, not passes.
- OPE-420 anti-cheating rule: implementation self-score is never enough. Require independent verification and hidden/adversarial/verifier-owned checks where practical. If a fix learns the eval instead of solving the general problem, block and repair. Each substantial loop must record hypothesis, expected failure mode, diagnostic, metric/evidence delta, and next move; stalled loops must use a new hypothesis/non-obvious alternative/removal of eval-shaped artifacts, not the same idea harder.
- 2026-06-12 Current active PR cleanup priority remains Quillio backend PRs #79-#86 with failing unit tests, then the other actionable PRs enumerated by implementation-lead. Apply Codex-only routing unless Samuel explicitly changes it; do not use Gemini or Claude for this lane.
- Before any GitHub write related to this lane, require `gh api user --jq .login` to return exactly `Samueljug`.
- Follow-up rule is event-first with a 30-minute fallback watchdog: act immediately on material progress, failure, completion evidence, or concrete blockers; report to Samuel immediately when material/completion/decision-needed; use 30-minute checks only when no trustworthy update arrives, a handoff may be stale, or progress is unclear; at watchdog checks verify source-of-truth state and unblock/reroute/restart/fix handoff/escalate as needed; stop only when the original requested outcome is complete, cancelled, superseded, or concretely blocked on Samuel.
