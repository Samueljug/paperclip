# Task (run non-interactively — never ask for permission or confirmation; just do it)

You are running headless inside a git repository, which is your current working directory.

Do this:
1. In `math.js`, add and export a function `add(a, b)` that returns `a + b` (keep it a CommonJS export).
2. Create `math.test.js` that uses Node's built-in `assert` to check `add(2, 3) === 5`, and run it with `node math.test.js` to confirm it passes.
3. Stage and commit your changes with a clear message.

When finished, output ONE short paragraph summarising exactly what you changed and the test result. Then stop.
